import React from "react";
import Gallery from "react-photo-gallery";
import { photos } from "@/data/photos";

const GalleryImages = () =>  <Gallery photos={photos} />;

export default GalleryImages;
